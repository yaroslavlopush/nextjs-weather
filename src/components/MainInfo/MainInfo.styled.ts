import styled from 'styled-components';

const StyledContainer = styled.div`
    margin-bottom: 32px;

    h1 {
        margin-bottom: 8px;
    }
`;

const StyledTextBlock = styled.div`
  font-size: 18px;
  color: #aaa;
`

export { StyledContainer, StyledTextBlock };

