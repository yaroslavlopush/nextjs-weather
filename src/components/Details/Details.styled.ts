import styled from 'styled-components';

const StyledContainer = styled.div`
    display: flex;
`;

const StyledDiv = styled.div`
    text-align: left;
    margin-left: 10px;
`;

const StyledValue = styled.div`
    font-weight: bold;
    font-size: 20px;
`;

const StyledText = styled.div`
  color: #aaa;
  font-size: 14px;
`;

export {  StyledContainer, StyledDiv, StyledValue, StyledText }
